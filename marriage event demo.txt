import React, { useState, useEffect } from 'react';
import { 
  Heart, 
  Calendar, 
  Users, 
  DollarSign, 
  CheckSquare, 
  Music, 
  Camera, 
  Utensils, 
  MapPin, 
  Star, 
  ChevronRight, 
  Menu, 
  X,
  PieChart,
  LogOut,
  Plus,
  Trash2,
  Check
} from 'lucide-react';

/**
 * UCD EVENTS - Professional Wedding & Event Management Platform
 * * Features:
 * - High-end Landing Page with Service Showcase
 * - Interactive Client Dashboard
 * - Guest List Management (Add/Remove/RSVP)
 * - Budget Tracking with Visual Bars
 * - Dynamic Checklist
 */

// --- Component: Navigation ---
const Navbar = ({ activeView, setActiveView, toggleDashboard }) => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  return (
    <nav className="fixed top-0 left-0 right-0 z-50 bg-slate-900/95 backdrop-blur-md text-white border-b border-slate-800 shadow-lg">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <div className="flex items-center cursor-pointer" onClick={() => setActiveView('landing')}>
            <div className="w-10 h-10 bg-amber-500 rounded-tr-xl rounded-bl-xl flex items-center justify-center mr-3">
              <span className="text-slate-900 font-bold text-xl">U</span>
            </div>
            <div>
              <h1 className="text-2xl font-serif tracking-widest font-bold">UCD<span className="text-amber-500">EVENTS</span></h1>
              <p className="text-[10px] uppercase tracking-widest text-slate-400">Exquisite Weddings</p>
            </div>
          </div>

          {/* Desktop Menu */}
          <div className="hidden md:flex space-x-8 items-center">
            <button onClick={() => setActiveView('landing')} className={`hover:text-amber-400 transition-colors ${activeView === 'landing' ? 'text-amber-500' : 'text-slate-300'}`}>Home</button>
            <button onClick={() => setActiveView('services')} className={`hover:text-amber-400 transition-colors ${activeView === 'services' ? 'text-amber-500' : 'text-slate-300'}`}>Facilities</button>
            <button onClick={() => setActiveView('portfolio')} className={`hover:text-amber-400 transition-colors ${activeView === 'portfolio' ? 'text-amber-500' : 'text-slate-300'}`}>Portfolio</button>
            
            <button 
              onClick={toggleDashboard}
              className="px-6 py-2 bg-gradient-to-r from-amber-500 to-amber-600 text-slate-900 font-bold rounded-full hover:shadow-[0_0_15px_rgba(245,158,11,0.5)] transition-all transform hover:-translate-y-0.5 flex items-center gap-2"
            >
              {activeView === 'dashboard' ? 'Sign Out' : 'Client Login'}
              {activeView !== 'dashboard' && <ChevronRight size={16} />}
            </button>
          </div>

          {/* Mobile Menu Button */}
          <div className="md:hidden">
            <button onClick={() => setIsMenuOpen(!isMenuOpen)} className="text-slate-300 hover:text-white">
              {isMenuOpen ? <X size={28} /> : <Menu size={28} />}
            </button>
          </div>
        </div>
      </div>

      {/* Mobile Dropdown */}
      {isMenuOpen && (
        <div className="md:hidden bg-slate-800 border-t border-slate-700">
          <div className="px-2 pt-2 pb-3 space-y-1 sm:px-3">
            <button onClick={() => {setActiveView('landing'); setIsMenuOpen(false)}} className="block px-3 py-2 text-base font-medium text-slate-300 hover:text-amber-400 w-full text-left">Home</button>
            <button onClick={() => {setActiveView('services'); setIsMenuOpen(false)}} className="block px-3 py-2 text-base font-medium text-slate-300 hover:text-amber-400 w-full text-left">Facilities</button>
            <button 
              onClick={() => {toggleDashboard(); setIsMenuOpen(false)}} 
              className="w-full text-left mt-4 px-5 py-3 bg-amber-500 text-slate-900 font-bold rounded-md"
            >
              {activeView === 'dashboard' ? 'Exit Dashboard' : 'Client Portal'}
            </button>
          </div>
        </div>
      )}
    </nav>
  );
};

// --- Component: Hero Section ---
const Hero = ({ onCtaClick }) => (
  <div className="relative h-screen flex items-center justify-center overflow-hidden bg-slate-900">
    {/* Abstract Background */}
    <div className="absolute inset-0 opacity-30">
        <div className="absolute top-0 left-0 w-full h-full bg-[radial-gradient(circle_at_50%_50%,rgba(245,158,11,0.15),transparent_50%)]"></div>
        <div className="absolute -top-40 -right-40 w-96 h-96 bg-amber-600 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob"></div>
        <div className="absolute -bottom-40 -left-40 w-96 h-96 bg-purple-600 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
    </div>
    
    <div className="relative z-10 text-center px-4 max-w-4xl mx-auto mt-16">
      <div className="inline-block mb-4 px-4 py-1 border border-amber-500/50 rounded-full bg-slate-800/50 backdrop-blur-sm text-amber-400 text-sm tracking-widest uppercase animate-fade-in-up">
        Premier Wedding Architect
      </div>
      <h1 className="text-5xl md:text-7xl font-serif font-bold text-white mb-6 leading-tight animate-fade-in-up animation-delay-100">
        Crafting Moments into <br />
        <span className="text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-amber-400 to-amber-600">Eternal Memories</span>
      </h1>
      <p className="text-lg md:text-xl text-slate-300 mb-10 font-light max-w-2xl mx-auto animate-fade-in-up animation-delay-200">
        UCD Events provides an orchestrated symphony of high-end facilities, from royal venues to gourmet catering, ensuring your special day is nothing short of perfection.
      </p>
      <div className="flex flex-col md:flex-row gap-4 justify-center animate-fade-in-up animation-delay-300">
        <button onClick={onCtaClick} className="px-8 py-4 bg-amber-500 hover:bg-amber-400 text-slate-900 font-bold rounded-lg shadow-[0_0_20px_rgba(245,158,11,0.3)] transition-all transform hover:scale-105">
          Plan Your Wedding
        </button>
        <button className="px-8 py-4 bg-transparent border border-slate-600 text-white hover:border-amber-500 hover:text-amber-400 font-medium rounded-lg transition-all backdrop-blur-sm">
          View Our Venues
        </button>
      </div>
    </div>
  </div>
);

// --- Component: Facilities/Services (INTERACTIVE UPDATE) ---
const Services = () => {
  const [activeService, setActiveService] = useState(0);

  const services = [
    { 
      id: 0,
      icon: <MapPin size={48} />, 
      title: "Royal Venues", 
      short: "Palaces & Resorts",
      desc: "Experience the grandeur of a lifetime. We provide exclusive access to heritage palaces, sun-kissed beach resorts, and 5-star banquet halls. Whether you desire a sunset wedding in Goa or a royal procession in Udaipur, our venue partners ensure the perfect backdrop for your vows.",
      theme: "from-amber-900 to-slate-900",
      accent: "text-amber-400",
      btnColor: "bg-amber-600 hover:bg-amber-500"
    },
    { 
      id: 1,
      icon: <Utensils size={48} />, 
      title: "Gourmet Catering", 
      short: "Global Cuisines",
      desc: "A culinary journey that delights every palate. Our master chefs curate bespoke menus featuring authentic regional delicacies, live interactive food stations, and global fusion masterpieces. We ensure hygiene, presentation, and taste are world-class.",
      theme: "from-rose-900 to-slate-900",
      accent: "text-rose-400",
      btnColor: "bg-rose-600 hover:bg-rose-500"
    },
    { 
      id: 2,
      icon: <Camera size={48} />, 
      title: "Cinematography", 
      short: "Photo & Video",
      desc: "Capturing your love story, frame by frame. Our award-winning cinematographers use state-of-the-art drone technology and 4K cameras to create a movie-like experience. We focus on candid moments, ensuring every tear, smile, and laugh is preserved forever.",
      theme: "from-blue-900 to-slate-900",
      accent: "text-blue-400",
      btnColor: "bg-blue-600 hover:bg-blue-500"
    },
    { 
      id: 3,
      icon: <Star size={48} />, 
      title: "Luxury Decor", 
      short: "Themes & Florals",
      desc: "Transforming spaces into dreamscapes. From vintage floral arches to futuristic LED stage setups, our designers bring your vision to life. We specialize in thematic weddings, using imported flowers, intelligent lighting, and luxurious fabrics.",
      theme: "from-fuchsia-900 to-slate-900",
      accent: "text-fuchsia-400",
      btnColor: "bg-fuchsia-600 hover:bg-fuchsia-500"
    },
    { 
      id: 4,
      icon: <Music size={48} />, 
      title: "Entertainment", 
      short: "Music & Dance",
      desc: "The heartbeat of your celebration. We arrange celebrity DJ performances, live classical bands, energetic dhol troupes, and choreographed dance sequences. We ensure your guests are entertained and the dance floor is never empty.",
      theme: "from-violet-900 to-slate-900",
      accent: "text-violet-400",
      btnColor: "bg-violet-600 hover:bg-violet-500"
    },
    { 
      id: 5,
      icon: <Users size={48} />, 
      title: "Guest Logistics", 
      short: "Hospitality",
      desc: "Seamless comfort for your loved ones. We manage end-to-end hospitality, including airport transfers in luxury fleets, hotel check-ins, and 24/7 concierge services for your guests. We treat your guests like our own family.",
      theme: "from-emerald-900 to-slate-900",
      accent: "text-emerald-400",
      btnColor: "bg-emerald-600 hover:bg-emerald-500"
    },
  ];

  const activeData = services[activeService];

  return (
    <div className="py-24 bg-slate-50 text-slate-900" id="facilities">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-12">
          <h2 className="text-amber-600 font-bold tracking-widest uppercase mb-2">Why Choose UCD Events</h2>
          <h3 className="text-4xl font-serif font-bold text-slate-900">World-Class Facilities</h3>
          <div className="w-24 h-1 bg-amber-500 mx-auto mt-4 rounded-full"></div>
        </div>

        {/* Dynamic Interactive Section */}
        <div className="rounded-3xl overflow-hidden shadow-2xl bg-white border border-slate-200 flex flex-col lg:flex-row min-h-[600px]">
          
          {/* Navigation Sidebar */}
          <div className="lg:w-1/3 bg-slate-100 border-r border-slate-200 p-6 flex flex-col gap-2 overflow-y-auto">
            <h4 className="text-slate-400 text-xs font-bold uppercase tracking-wider mb-4 px-2">Select Service</h4>
            {services.map((service, index) => (
              <button
                key={index}
                onClick={() => setActiveService(index)}
                className={`w-full text-left px-6 py-4 rounded-xl transition-all duration-300 flex items-center justify-between group ${
                  activeService === index 
                    ? `bg-white shadow-lg border-l-4 ${service.accent.replace('text', 'border')} ring-1 ring-slate-200` 
                    : 'hover:bg-white hover:shadow-md text-slate-500'
                }`}
              >
                <div>
                    <span className={`block font-bold text-lg ${activeService === index ? 'text-slate-800' : 'text-slate-600'}`}>{service.title}</span>
                    <span className="text-xs text-slate-400 font-medium">{service.short}</span>
                </div>
                {activeService === index && <ChevronRight size={20} className={service.accent} />}
              </button>
            ))}
          </div>

          {/* Content Display Area */}
          <div className={`lg:w-2/3 p-12 flex flex-col justify-center items-start text-white transition-all duration-500 bg-gradient-to-br ${activeData.theme}`}>
            <div className={`p-4 rounded-2xl bg-white/10 backdrop-blur-md mb-6 animate-fade-in ${activeData.accent}`}>
                {activeData.icon}
            </div>
            
            <h2 className="text-4xl md:text-5xl font-serif font-bold mb-6 leading-tight animate-fade-in">
                {activeData.title}
            </h2>
            
            <p className="text-lg text-slate-200 leading-relaxed mb-10 max-w-2xl animate-fade-in animation-delay-100">
                {activeData.desc}
            </p>
            
            <button className={`px-8 py-4 rounded-lg font-bold text-white shadow-lg transition-transform hover:scale-105 ${activeData.btnColor} animate-fade-in animation-delay-200`}>
                Book {activeData.title}
            </button>

            <div className="mt-auto pt-12 w-full border-t border-white/10 flex justify-between text-sm text-slate-400">
                <span>UCD Exclusive</span>
                <span>Premium Quality Guaranteed</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

// --- Component: Dashboard (The "Product") ---
const Dashboard = ({ user }) => {
  const [activeTab, setActiveTab] = useState('overview');

  // Interactive State for Dashboard Demo
  const [guests, setGuests] = useState([
    { id: 1, name: "Mr. & Mrs. Sharma", rsvp: "Confirmed", table: 4, count: 2 },
    { id: 2, name: "The Kapoor Family", rsvp: "Pending", table: 1, count: 4 },
    { id: 3, name: "John Doe", rsvp: "Declined", table: null, count: 1 },
    { id: 4, name: "Sarah Smith", rsvp: "Confirmed", table: 2, count: 1 },
  ]);
  const [newGuestName, setNewGuestName] = useState("");
  
  const [budget, setBudget] = useState([
    { category: "Venue", allocated: 15000, spent: 15000 },
    { category: "Catering", allocated: 12000, spent: 8000 },
    { category: "Decor", allocated: 8000, spent: 4500 },
    { category: "Attire", allocated: 5000, spent: 5200 },
  ]);

  const [todos, setTodos] = useState([
    { id: 1, text: "Finalize Venue Contract", done: true },
    { id: 2, text: "Menu Tasting Session", done: true },
    { id: 3, text: "Send Invitations", done: false },
    { id: 4, text: "Book Makeup Artist", done: false },
  ]);

  // Helper Functions
  const addGuest = () => {
    if (newGuestName.trim()) {
      setGuests([...guests, { id: Date.now(), name: newGuestName, rsvp: "Pending", table: null, count: 1 }]);
      setNewGuestName("");
    }
  };

  const deleteGuest = (id) => {
    setGuests(guests.filter(g => g.id !== id));
  };

  const toggleTodo = (id) => {
    setTodos(todos.map(t => t.id === id ? { ...t, done: !t.done } : t));
  };

  const calculateTotalBudget = () => budget.reduce((acc, curr) => acc + curr.allocated, 0);
  const calculateTotalSpent = () => budget.reduce((acc, curr) => acc + curr.spent, 0);

  // Sub-components for Dashboard
  const SidebarItem = ({ id, icon, label }) => (
    <button 
      onClick={() => setActiveTab(id)}
      className={`w-full flex items-center space-x-3 px-6 py-4 transition-colors border-l-4 ${
        activeTab === id 
          ? 'bg-amber-50 border-amber-500 text-amber-700' 
          : 'border-transparent text-slate-500 hover:bg-slate-50 hover:text-slate-800'
      }`}
    >
      {icon}
      <span className="font-medium">{label}</span>
    </button>
  );

  return (
    <div className="pt-20 min-h-screen bg-slate-100 flex flex-col md:flex-row">
      {/* Dashboard Sidebar */}
      <aside className="w-full md:w-64 bg-white shadow-xl z-10 md:h-[calc(100vh-80px)] md:fixed md:top-20">
        <div className="p-6 border-b border-slate-100">
          <div className="flex items-center space-x-3">
            <div className="h-12 w-12 rounded-full bg-slate-200 flex items-center justify-center text-slate-600 font-bold text-lg">
              JS
            </div>
            <div>
              <h3 className="font-bold text-slate-800">James & Sarah</h3>
              <p className="text-xs text-slate-500">Dec 15, 2026</p>
            </div>
          </div>
        </div>
        <nav className="mt-4">
          <SidebarItem id="overview" icon={<PieChart size={20} />} label="Overview" />
          <SidebarItem id="guests" icon={<Users size={20} />} label="Guest List" />
          <SidebarItem id="budget" icon={<DollarSign size={20} />} label="Budget Tracker" />
          <SidebarItem id="checklist" icon={<CheckSquare size={20} />} label="Checklist" />
        </nav>
      </aside>

      {/* Main Content Area */}
      <main className="flex-1 md:ml-64 p-4 md:p-8 overflow-y-auto">
        
        {/* VIEW: OVERVIEW */}
        {activeTab === 'overview' && (
          <div className="space-y-6 animate-fade-in">
            <h2 className="text-3xl font-serif font-bold text-slate-800">Welcome to your Dashboard</h2>
            
            {/* Stats Grid */}
            <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
              <div className="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
                <div className="flex justify-between items-start">
                  <div>
                    <p className="text-slate-500 text-sm font-medium">Days to Go</p>
                    <h3 className="text-4xl font-bold text-slate-800 mt-2">124</h3>
                  </div>
                  <div className="p-3 bg-blue-50 text-blue-600 rounded-lg"><Calendar size={24} /></div>
                </div>
              </div>
              <div className="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
                <div className="flex justify-between items-start">
                  <div>
                    <p className="text-slate-500 text-sm font-medium">Confirmed Guests</p>
                    <h3 className="text-4xl font-bold text-slate-800 mt-2">
                      {guests.filter(g => g.rsvp === 'Confirmed').length}
                    </h3>
                  </div>
                  <div className="p-3 bg-emerald-50 text-emerald-600 rounded-lg"><Users size={24} /></div>
                </div>
              </div>
              <div className="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
                <div className="flex justify-between items-start">
                  <div>
                    <p className="text-slate-500 text-sm font-medium">Tasks Completed</p>
                    <h3 className="text-4xl font-bold text-slate-800 mt-2">
                       {Math.round((todos.filter(t => t.done).length / todos.length) * 100)}%
                    </h3>
                  </div>
                  <div className="p-3 bg-amber-50 text-amber-600 rounded-lg"><CheckSquare size={24} /></div>
                </div>
              </div>
            </div>

            {/* Recent Activity / Alert */}
            <div className="bg-gradient-to-r from-slate-800 to-slate-900 rounded-xl p-8 text-white shadow-lg">
              <h3 className="text-xl font-bold mb-2">Vendor Appointment</h3>
              <p className="text-slate-300 mb-4">You have a tasting session with "The Royal Kitchens" scheduled for tomorrow at 2:00 PM.</p>
              <button className="px-4 py-2 bg-amber-500 text-slate-900 font-bold rounded hover:bg-amber-400 transition-colors">View Details</button>
            </div>
          </div>
        )}

        {/* VIEW: GUESTS */}
        {activeTab === 'guests' && (
          <div className="bg-white rounded-xl shadow-sm border border-slate-200 p-6 animate-fade-in">
            <div className="flex justify-between items-center mb-6">
              <h2 className="text-2xl font-bold text-slate-800">Guest List Management</h2>
              <span className="bg-slate-100 text-slate-600 px-3 py-1 rounded-full text-sm font-medium">Total: {guests.length}</span>
            </div>

            {/* Add Guest Form */}
            <div className="flex gap-2 mb-6">
              <input 
                type="text" 
                placeholder="Enter guest name..." 
                className="flex-1 border border-slate-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-amber-500 focus:border-amber-500 outline-none"
                value={newGuestName}
                onChange={(e) => setNewGuestName(e.target.value)}
                onKeyPress={(e) => e.key === 'Enter' && addGuest()}
              />
              <button onClick={addGuest} className="bg-amber-500 hover:bg-amber-600 text-white px-6 py-2 rounded-lg font-medium transition-colors flex items-center">
                <Plus size={18} className="mr-2" /> Add
              </button>
            </div>

            {/* Guest Table */}
            <div className="overflow-x-auto">
              <table className="w-full">
                <thead className="bg-slate-50 border-b border-slate-200">
                  <tr>
                    <th className="text-left py-3 px-4 font-semibold text-slate-600">Guest Name</th>
                    <th className="text-left py-3 px-4 font-semibold text-slate-600">RSVP Status</th>
                    <th className="text-left py-3 px-4 font-semibold text-slate-600">Table No</th>
                    <th className="text-right py-3 px-4 font-semibold text-slate-600">Actions</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-slate-100">
                  {guests.map(guest => (
                    <tr key={guest.id} className="hover:bg-slate-50 transition-colors">
                      <td className="py-3 px-4 text-slate-800 font-medium">{guest.name}</td>
                      <td className="py-3 px-4">
                        <span className={`px-2 py-1 rounded-full text-xs font-semibold ${
                          guest.rsvp === 'Confirmed' ? 'bg-emerald-100 text-emerald-700' :
                          guest.rsvp === 'Declined' ? 'bg-red-100 text-red-700' :
                          'bg-amber-100 text-amber-700'
                        }`}>
                          {guest.rsvp}
                        </span>
                      </td>
                      <td className="py-3 px-4 text-slate-500">{guest.table ? `Table ${guest.table}` : '-'}</td>
                      <td className="py-3 px-4 text-right">
                        <button onClick={() => deleteGuest(guest.id)} className="text-slate-400 hover:text-red-500 transition-colors">
                          <Trash2 size={18} />
                        </button>
                      </td>
                    </tr>
                  ))}
                  {guests.length === 0 && (
                    <tr>
                      <td colSpan="4" className="text-center py-8 text-slate-400">No guests added yet. Start by adding a name above.</td>
                    </tr>
                  )}
                </tbody>
              </table>
            </div>
          </div>
        )}

        {/* VIEW: BUDGET */}
        {activeTab === 'budget' && (
          <div className="space-y-6 animate-fade-in">
             <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <div className="bg-gradient-to-br from-emerald-500 to-emerald-700 rounded-xl p-6 text-white shadow-lg">
                  <p className="text-emerald-100 mb-1">Total Budget</p>
                  <h3 className="text-3xl font-bold">${calculateTotalBudget().toLocaleString()}</h3>
                </div>
                <div className="bg-white rounded-xl p-6 border border-slate-200 shadow-sm">
                  <p className="text-slate-500 mb-1">Total Spent</p>
                  <div className="flex items-end gap-3">
                    <h3 className="text-3xl font-bold text-slate-800">${calculateTotalSpent().toLocaleString()}</h3>
                    <span className={`mb-1 font-medium ${calculateTotalSpent() > calculateTotalBudget() ? 'text-red-500' : 'text-emerald-500'}`}>
                      {Math.round((calculateTotalSpent() / calculateTotalBudget()) * 100)}% Used
                    </span>
                  </div>
                </div>
             </div>

             <div className="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
               <h3 className="text-xl font-bold text-slate-800 mb-6">Expense Breakdown</h3>
               <div className="space-y-6">
                 {budget.map((item, index) => {
                   const percentage = Math.min((item.spent / item.allocated) * 100, 100);
                   const isOverBudget = item.spent > item.allocated;
                   
                   return (
                     <div key={index}>
                       <div className="flex justify-between mb-2">
                         <span className="font-medium text-slate-700">{item.category}</span>
                         <span className="text-sm text-slate-500">
                           ${item.spent.toLocaleString()} / <span className="text-slate-400">${item.allocated.toLocaleString()}</span>
                         </span>
                       </div>
                       <div className="h-3 w-full bg-slate-100 rounded-full overflow-hidden">
                         <div 
                            className={`h-full rounded-full transition-all duration-1000 ${isOverBudget ? 'bg-red-500' : 'bg-amber-500'}`}
                            style={{ width: `${percentage}%` }}
                         ></div>
                       </div>
                       {isOverBudget && <p className="text-xs text-red-500 mt-1">Over budget by ${item.spent - item.allocated}</p>}
                     </div>
                   );
                 })}
               </div>
             </div>
          </div>
        )}

        {/* VIEW: CHECKLIST */}
        {activeTab === 'checklist' && (
          <div className="bg-white rounded-xl shadow-sm border border-slate-200 p-6 animate-fade-in max-w-3xl mx-auto">
             <div className="mb-8 text-center">
               <h2 className="text-2xl font-bold text-slate-800">Wedding Checklist</h2>
               <p className="text-slate-500">Keep track of your planning progress</p>
             </div>

             <div className="space-y-3">
               {todos.map(todo => (
                 <div 
                    key={todo.id} 
                    onClick={() => toggleTodo(todo.id)}
                    className={`flex items-center p-4 rounded-lg border cursor-pointer transition-all ${
                      todo.done 
                        ? 'bg-slate-50 border-slate-200 opacity-75' 
                        : 'bg-white border-slate-200 hover:border-amber-400 shadow-sm'
                    }`}
                 >
                   <div className={`w-6 h-6 rounded-md border flex items-center justify-center mr-4 transition-colors ${
                     todo.done ? 'bg-emerald-500 border-emerald-500 text-white' : 'border-slate-300 bg-white'
                   }`}>
                     {todo.done && <Check size={16} />}
                   </div>
                   <span className={`text-lg ${todo.done ? 'line-through text-slate-400' : 'text-slate-800'}`}>
                     {todo.text}
                   </span>
                 </div>
               ))}
             </div>

             <div className="mt-8 pt-6 border-t border-slate-100 flex justify-center">
               <button className="text-amber-600 font-medium hover:text-amber-700 flex items-center">
                 <Plus size={18} className="mr-1" /> Add Custom Task
               </button>
             </div>
          </div>
        )}

      </main>
    </div>
  );
};

// --- Main Application Wrapper ---
const App = () => {
  const [activeView, setActiveView] = useState('landing'); // 'landing', 'services', 'dashboard', 'portfolio'
  
  // Handlers
  const handleNav = (view) => setActiveView(view);
  const toggleDashboard = () => setActiveView(activeView === 'dashboard' ? 'landing' : 'dashboard');

  return (
    <div className="font-sans text-slate-900 bg-slate-50 min-h-screen">
      <Navbar activeView={activeView} setActiveView={setActiveView} toggleDashboard={toggleDashboard} />
      
      {activeView === 'dashboard' ? (
        <Dashboard user={{ name: "Client" }} />
      ) : (
        <>
          {activeView === 'landing' && <Hero onCtaClick={() => handleNav('services')} />}
          
          {(activeView === 'landing' || activeView === 'services') && <Services />}

          {/* Contact / Footer Section */}
          <footer className="bg-slate-900 text-white py-16">
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid grid-cols-1 md:grid-cols-4 gap-12">
              <div className="col-span-1 md:col-span-2">
                <h2 className="text-3xl font-serif font-bold mb-6">UCD<span className="text-amber-500">EVENTS</span></h2>
                <p className="text-slate-400 mb-6 max-w-sm">
                  We don't just plan weddings; we design experiences that linger in the heart forever. Contact us to begin your journey.
                </p>
                <div className="flex space-x-4">
                  <div className="w-10 h-10 rounded-full bg-slate-800 flex items-center justify-center hover:bg-amber-500 hover:text-slate-900 transition-colors cursor-pointer">
                    <Music size={18} />
                  </div>
                  <div className="w-10 h-10 rounded-full bg-slate-800 flex items-center justify-center hover:bg-amber-500 hover:text-slate-900 transition-colors cursor-pointer">
                    <Camera size={18} />
                  </div>
                  <div className="w-10 h-10 rounded-full bg-slate-800 flex items-center justify-center hover:bg-amber-500 hover:text-slate-900 transition-colors cursor-pointer">
                    <Heart size={18} />
                  </div>
                </div>
              </div>
              
              <div>
                <h4 className="text-lg font-bold mb-6 text-amber-500">Contact</h4>
                <ul className="space-y-4 text-slate-400">
                  <li className="flex items-center"><MapPin size={16} className="mr-2 text-amber-500"/> 123 Royal Avenue, Estate District</li>
                  <li className="flex items-center"><Utensils size={16} className="mr-2 text-amber-500"/> contact@ucdevents.com</li>
                  <li className="flex items-center"><Users size={16} className="mr-2 text-amber-500"/> +1 (800) 123-4567</li>
                </ul>
              </div>

              <div>
                <h4 className="text-lg font-bold mb-6 text-amber-500">Office Hours</h4>
                <ul className="space-y-2 text-slate-400">
                  <li>Mon - Fri: 9:00 AM - 8:00 PM</li>
                  <li>Saturday: 10:00 AM - 6:00 PM</li>
                  <li>Sunday: By Appointment</li>
                </ul>
              </div>
            </div>
            <div className="border-t border-slate-800 mt-12 pt-8 text-center text-slate-600 text-sm">
              &copy; 2024 UCD Events Management. All rights reserved.
            </div>
          </footer>
        </>
      )}
    </div>
  );
};

export default App;